# A stray packed extension bundle elsewhere is build output, not source — but
# downloads/*.zip is the deliberate exception: index.html's Download buttons link to
# those, so they're committed on purpose. Regenerate them with scripts/build-zips.sh.
*.zip
!/downloads/*.zip

.DS_Store
node_modules/

# Agent working files — plans, research, generated output.
.agents/
